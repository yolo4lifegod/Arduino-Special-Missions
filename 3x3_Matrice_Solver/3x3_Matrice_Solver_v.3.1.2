#include <BasicLinearAlgebra.h>
using namespace BLA;

#include <LiquidCrystal.h>
LiquidCrystal lcd(12, 11, 5, 4, 3, 2);

int potPin = 2;
int a;

void setup(){
  BLA::Matrix<a,a> A;
  BLA::Matrix<a> B;
  BLA::Matrix<a,a> C;
  BLA::Matrix<a,1> D;
}
